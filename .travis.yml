language: c
dist: trusty
before_install:
  - sudo apt-get update -qq
install:
  - sudo apt-get install libcap-dev libidn2-0-dev nettle-dev -qq

compiler:
  - gcc
  - clang
script: ./autogen.sh && ./configure --enable-rdisc-server --enable-ninfod --enable-pthreads && make

